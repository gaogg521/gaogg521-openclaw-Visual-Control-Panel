# redist（便携 Node + MinGit）

此目录由 **`../prepare-full-redist.ps1`** 自动填充，**不要手工提交大文件到 Git**。

生成后应存在：

- `node/node.exe` — Node.js Windows x64 便携版  
- `git/cmd/git.exe` — MinGit  

然后再编译 **`OpenClawOneClick.Full.iss`**。
