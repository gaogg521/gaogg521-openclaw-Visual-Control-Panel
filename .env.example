# openclaw CLI 可执行文件的完整路径（写入配置、Gateway call 时需要）。
# 从 Cursor/IDE 启动 npm run dev 时，进程 PATH 往往不含 npm 全局目录，会出现 spawn openclaw ENOENT。
# Windows 典型路径（请改成你的用户名）：
# OPENCLAW_CLI=C:/Users/你的用户名/AppData/Roaming/npm/openclaw.cmd
# macOS Homebrew 示例：
# OPENCLAW_CLI=/opt/homebrew/bin/openclaw
# 也可用别名：OPENCLAW_CLI_PATH（与 OPENCLAW_CLI 二选一即可）
#
# 更推荐（Windows 避免 cmd 破坏 gateway --params 内 JSON）：直接指向包内入口脚本
# OPENCLAW_MJS=C:/Users/你的用户名/AppData/Roaming/npm/node_modules/openclaw/openclaw.mjs
# （与 OPENCLAW_ENTRY 同义，二选一）

# OpenClaw 根目录。仪表盘将从此目录读取 openclaw.json、agents、sessions 等。
# 注意：openclaw.json 必须是标准 JSON（不能有 // 或 /* */ 注释、不能有尾随逗号），否则 OpenClaw/仪表盘解析会失败。
# 不设置时默认使用：~/.openclaw（即用户目录下的 .openclaw）
#
# 示例（按需取消注释并修改）：
# Linux/macOS:
# OPENCLAW_HOME=/home/you/.openclaw
#
# Windows（建议使用正斜杠）:
# OPENCLAW_HOME=C:/Users/你的用户名/.openclaw
#
# OPENCLAW_HOME=C:/Users/allenzhao/.openclaw
#
# 像素办公室布局持久化目录（可选，首次保存布局时会自动创建）：
#   %OPENCLAW_HOME%/pixel-office/layout.json
# 与仓库根目录下的 openclaw-office/ 等独立工程无关。

# MySQL 持久化（可选，默认值已可直连本机 root/root）
# MYSQL_HOST=127.0.0.1
# MYSQL_PORT=3306
# MYSQL_USER=root
# MYSQL_PASSWORD=root
# MYSQL_DATABASE=openclaw_visualization
